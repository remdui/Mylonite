# Optional: Mylonite will generate runtime/config/.env on first start if it is missing
# or if DJANGO_SECRET_KEY is empty.
# Set DJANGO_DEBUG=true only for local development.
# Optional during key rotation:
# DJANGO_SECRET_KEY_FALLBACKS=old-key-1,old-key-2

DJANGO_SECRET_KEY=
DJANGO_DEBUG=false
